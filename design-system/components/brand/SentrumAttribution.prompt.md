"Powered by Sentrum" footer row — used at the bottom of the sidebar in white-label tenant deployments.

```jsx
<SentrumAttribution />
```

Renders at 50% opacity (90% on hover) so it blends into chrome. Don't place it on dark surfaces without checking contrast.
