/** User chat message — gray-100/80 bubble with avatar inside. Assistant replies have NO bubble. */
export interface UserMessageProps {
  children?: React.ReactNode
  className?: string
  style?: React.CSSProperties
}
