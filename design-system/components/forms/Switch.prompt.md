Boolean toggle for settings rows and feature flags.

```jsx
<Switch defaultChecked onCheckedChange={v => console.log(v)} />
```

Controlled (`checked` + `onCheckedChange`) or uncontrolled (`defaultChecked`).
