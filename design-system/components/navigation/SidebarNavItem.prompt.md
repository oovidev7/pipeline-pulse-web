A single nav row inside a sidebar section.

```jsx
<SidebarNavItem icon={<FileTextIcon />} label="Reports" active />
<SidebarNavItem icon={<FilePlusIcon />} label="New Report" onClick={go} />
```

Icons are 14px Lucide outlines. Active state = gray-100 pill + medium weight, not a color change.
