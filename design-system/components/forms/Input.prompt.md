Single-line text input used across forms and settings.

```jsx
<Label htmlFor="email">Email</Label>
<Input id="email" type="email" placeholder="anna@club.com" />
```

Pairs with `Label`. 36px tall; placeholder is gray-400; focus shows a soft 3px ring.
