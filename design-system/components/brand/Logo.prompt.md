The Sentrum logo lockup — stylized "S" mark with the lowercase "sentrum" wordmark set in Outfit medium with tight tracking.

```jsx
<Logo />                              // icon + wordmark
<Logo variant="icon" size="lg" />     // mark only
<Logo variant="wordmark" />           // text only
<Logo theme="dark" />                 // white, for dark surfaces
```

Notes: the wordmark is always lowercase. On dark/navy surfaces pass `theme="dark"`. Sizes: xs (12px) through xl (48px).
