/** Typing indicator — pulsing slate Sentrum "S" mark with soft glow. */
export interface TypingIndicatorProps {
  /** Pixel size. @default 32 */
  size?: number
  className?: string
  style?: React.CSSProperties
}
